# Lightweight client/test dependencies for the Vast T4 SGLang runtime.
#
# Preferred install entrypoint from the repo root:
#   python scripts/mednla/vast_bootstrap.py --profile t4-sglang --require-existing-torch
#
# Do not install this file as a replacement for the bootstrap helper unless
# the repo itself was already installed with:
#   python -m pip install --no-deps -e .
#
# Do not add torch or sglang[all] here. The SGLang template owns the
# SGLang/CUDA stack; this file stays client-side and bounded.

pytest
orjson
pyyaml
numpy
pyarrow
httpx
safetensors
huggingface_hub
transformers>=4.46,<5
